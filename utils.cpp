#include "utils.h"

void drawString(unsigned handle, const char * text, int score1, int score2, float x, float y, float width, float height, float angle, char offset, unsigned int tint, const float transform[16])
{
	// TODO: wrap the string func to handle things
}
